# Rust / Cargo
src-tauri/target/

# Node / Vite
node_modules/
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Data files (auto-generated next to the exe, already covered by src-tauri/target/)
# *.config and *.notes are intentionally NOT ignored globally — only the target/ directory

# Reference app — stale binaries & runtime data
this is the reference folder/Notes.exe
this is the reference folder/Notes.lnk
this is the reference folder/StickyNote.cfg
this is the reference folder/temp.json

# AI agent instructions (not part of the project source)
AGENTS.md

# Tauri generated schemas
src-tauri/gen/
